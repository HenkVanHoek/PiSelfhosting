name: Build Ubuntu Installer

on:
  release:
    types: [published]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller
          pip install -r requirements.txt

      - name: Build with PyInstaller (Linux)
        run: >-
          python -m PyInstaller --onefile
          --name "PiSelfhosting-Installer-Linux"
          --add-data "src:src"
          --add-data "templates:templates"
          --add-data "components_list.txt:."
          --add-data "config:config"
          # --add-data "selected_components.txt:."
          --add-data "Dockerfile.setup-tool:."
          piselfhosting_installer.py

      - name: Upload Release Asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ github.event.release.upload_url }}
          asset_path: ./dist/PiSelfhosting-Installer-Linux
          asset_name: PiSelfhosting-Installer-Linux
          asset_content_type: application/octet-stream